{% extends 'login.html.twig' %}

{% block title %}Bienvenue!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }


    .back_btn {
        color: black;
        background-color: #FFC312;
    }
    .back_btn:hover, .front_btn:hover{
        color: black;
        background-color: white;
    }
    .front_btn {
        color: black;
        background-color:     #009688
    }
    a {
        color: black;
    }
</style>

    <div class="container">
        <div class="d-flex justify-content-center h-100">
            <div class="card">
                <img src="{{ asset('img/login_header.png') }}" class="img-fluid" alt="{{ SITE_NAME }}">
                <div class="card-body">
                    <button class="btn btn-lg btn-block back_btn" type="submit"><a href="{{ path('sonata_admin_dashboard') }}">Accès à l'administration</a></button>
                    <button class="btn btn-lg btn-block front_btn" type="submit"><a href="{{ path('front') }}">Front Office</a></button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
