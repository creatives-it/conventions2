{#{% if admin.isGranted('EDIT', object) and admin.hasRoute('annoter') %}
    <a class="btn btn-sm btn-info edit_link" title="{{ 'Annoter'|trans }}"  style="display: inline-block" href="{{ admin.generateObjectUrl('annoter', object) }}">
        <i class="fa fa-inbox"></i> {{ 'Annoter'|trans }}
    </a>
{% endif %}#}

{% if admin.isGranted('EDIT', object) and admin.hasRoute('annoter') %}
    <a href="#"
       class="btn btn-sm btn-info edit_link"
       data-toggle="modal"
       data-target="#annoterModal-{{ object.id }}">
        <i class="fa fa-inbox" aria-hidden="true"></i>
        {{ 'Annoter'|trans }}
    </a>

    <div class="modal fade" id="annoterModal-{{ object.id }}" tabindex="-1" role="dialog" aria-labelledby="deleteLabel">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title" id="myModalLabel">{{ 'Annoter courrier'|trans }}</h4>
                </div>
                <div class="modal-body">
                    {{ render(controller('App\\Controller\\CourrierController::annoter', { 'id': object.id } )) }}
                </div>
                {#<div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'Annuler'|trans }}</button>
                    <a href="{{ admin.generateObjectUrl('annoter', object) }}"  class="btn btn-info"
                       title="{{ 'Annoter'|trans }}"> {{ 'Annoter'|trans }}
                    </a>
                </div>#}
            </div>
        </div>
    </div>
{% endif %}